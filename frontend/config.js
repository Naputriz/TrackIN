
// Kalau backend sudah bener, ganti API_BASE aja

const CONFIG = {
  // masih pake mock server lokal
  API_BASE: "http://127.0.0.1:8787",

  // backend sudah jalan → ganti ke ini
  // API_BASE: "http://localhost:8000",

  // sudah deploy → ganti ke ini:
  // API_BASE: "https://api.trackin.app",

  POLLING_INTERVAL: 5000,
};